<%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
  <%= f.search_field :body_cont, #aici e eroarea din search_controller
  placeholder: "Search...",
  class: "form-control me-2" %>
  <%= f.submit "Search!", class: "btn btn-ouline-succes" %>
<% end %>
